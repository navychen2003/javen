package org.javenstudio.provider.app.anybox.user;

import org.javenstudio.provider.account.notify.NotifyBinder;

public class AnyboxNotifyBinder extends NotifyBinder {
	//private static final Logger LOG = Logger.getLogger(AnyboxNotifyBinder.class);

	public AnyboxNotifyBinder(AnyboxNotifyProvider provider) {
		super(provider);
	}
	
}

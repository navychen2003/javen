package org.javenstudio.provider.account.list;

import android.app.Activity;

public interface OnAccountListClickListener {

	public boolean onAccountClick(Activity activity, AccountListItem item);
	
}
